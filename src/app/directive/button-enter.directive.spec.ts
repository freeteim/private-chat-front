import { ButtonEnterDirective } from './button-enter.directive';

describe('ButtonEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonEnterDirective();
    expect(directive).toBeTruthy();
  });
});
